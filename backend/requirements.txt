fastapi==0.115.12
uvicorn[standard]==0.34.2
SQLAlchemy==2.0.40
asyncpg==0.30.0
aiosqlite==0.20.0
pydantic[email]==2.11.4

# fastapi==0.115.12 targets starlette 0.41.x — pinning an upper bound keeps
# `pip install -r` from resolving a newer starlette that removed the
# `on_startup` / `on_shutdown` kwargs APIRouter still passes, which crashes
# import at startup.
starlette>=0.41.0,<0.47

# Auth: bcrypt for password storage (pinned to 4.0.x — 4.1 broke the
# passlib backend) and PyJWT for signed access tokens carrying sub+role+exp.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
PyJWT==2.8.0

# Rate limiting on auth endpoints (login, signup, refresh) so credential
# stuffing and brute force can't iterate without backoff.
slowapi==0.1.9

# AI tutor (Cora): async HTTP client used to call the OpenAI-compatible
# /chat/completions endpoint. Install httpx before enabling the chatbot.
httpx==0.28.1

