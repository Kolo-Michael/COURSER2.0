"""FastAPI application package (COURSER backend).

Contains the API router (`app.main`), per-resource endpoint modules
(`app.api`), configuration/env loading (`app.core`), SQLAlchemy models
(`app.models`), Pydantic request/response schemas (`app.schemas`), and
business logic (`app.services`).
"""